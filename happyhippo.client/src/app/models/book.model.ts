export interface IBook {
  title: string;
  author: string;
  year: number;
  user: string;
}

export interface IBookId {
  id: number;
  title: string;
  author: string;
  year: number;
  user: string;
}
